- **The shared Neumann enforcement imposed `du/dn = -g` at every low wall** (Issue #2141).
  - `enforce_neumann_value_nd` wrote the min side as `u[0] = u[1] - g*h`. That implements `du/dx = +g`, and against the outward normal `-x` it is `du/dn = -g` — the datum with its sign inverted. The max side was already correct, so the magnitude was right and only one wall was wrong. Both walls are `neighbour + g*h`; the asymmetry belongs to `du/dx`, not to the formula.
  - **Reached through the public API, and a 1-D measurement says otherwise.** Measured at `aad4aecd` on a 9-point 2-D grid, `HJBFDMSolver.solve_hjb_system` with `neumann_bc(value=0.7)`: low wall `-0.700000`, high wall `+0.700000`, against a requested `+0.7` at both. After the fix, `+0.700000` at both. In 1-D the same call enters the enforcement family **zero** times, so a 1-D population measures nothing and reports the code clean; `problem.solve()` never reaches it in any dimension, because `FPFDMSolver` refuses an inhomogeneous Neumann value first (#1686).
  - **Not dead code, checked rather than assumed.** Replacing `FDMApplicator.enforce_values` with an identity on its `field=` argument moves the 2-D **Dirichlet(1.0)** solution by **0.998320**, while leaving 2-D no-flux and all of 1-D bit-identical. Deleting the family — the other arm of the decision #2141 asked for — was therefore never available.

    Fixture, because these are measurements over one and not properties of the code: bounds $[0,1]^d$, 9 points per axis, `T=0.05`, `Nt=4`, `sigma=0.1`, `M = 0.5`, zero terminal and coupling fields, and the components built by `_default_components_2d()` / `_default_components()` in `tests/unit/test_alg/test_hjb_fdm_solver.py` — imported from that file rather than retyped, because the first version of this sentence cited it while the probe used its own Hamiltonian (`coupling = 0.5*m` against that file's `m`), and a reader rebuilding the stated recipe got a different number. The quantity is `max|live - neutralised|`.

    Under that fixture the 2-D Neumann row moves by **0.196442** before the fix and **0.174306** after; a single number for it would name neither tree. The call count is 4 and scales one-for-one with `Nt` (3, 4, 5, 8 at `Nt` = 3, 4, 5, 8), so it is a fact about the time grid rather than about the code.
  - **A third consumer, fixed by the same line and worth naming.** `enforce_robin_value_nd`'s pure-Neumann branch (`alpha` approximately 0) delegates to `enforce_neumann_value_nd` with `rhs_value / beta`, so the Robin wall carried the same inverted sign at the min side. Measured after the fix with `alpha=0, beta=2.0, g=1.4, h=0.5`: `du/dn = +0.700000` at **both** walls against the requested `g/beta = 0.7`. The two applicators are not the whole blast radius.
  - **The same decision, fixed once before.** #1265 corrected this exact low-wall sign on the sibling ghost path in `a0f40fe1`, whose message states the rule: *"the ghost cell must be u_interior + dx*v … had `-= dx*v` (implements du/dx=v, not du/dn=v). Fix: `+= dx*v`."* `enforcement.py` was not in that change's scope, so two implementations of one convention drifted apart. `git log -L` over the branch returns a single commit — it had never been touched since it was written.
  - **The wrong fix is live, and one assertion catches it.** Flipping both arms also makes the low wall read `+g`, and passes any check that looks only there. `test_neumann_wall_sign_2141.py`'s right-wall control asserts `+g` at the high wall for exactly that reason.
  - Retires the FDM half of `test_neumann_wall_sign_2141.py` (#2299) through its own mechanism: the `xfail(strict=True)` cell reported XPASS and the recorded-defect test failed carrying its delete instruction. The `InterpolationApplicator` half — `_enforce_boundary_1d` passing a literal `grad_value=0.0` to the Neumann branch, never reading the segment — is untouched and stays pinned. Cited by function rather than by line: the line number was right when written and is the kind of claim that expires silently.
