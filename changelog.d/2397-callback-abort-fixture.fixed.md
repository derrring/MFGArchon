`test_callback_abort_is_not_convergence_1684.py` measures its original problem again (#2397). Its fixture passed `coupling=lambda m: m`, which #2391 (#2375 ruling 3) turned into the opposite-signed problem without turning the test red. The test kept passing but stopped catching the `fp_initial_condition_written_at_final_index` mutation, and the first sharded discrimination sweep reported that as 130 -> 129. The fixture is now negated. `l2distu_rel` without a callback is `0.3832022588203827`, bit-identical to the tree before #2391, and the test fails under the mutation again with the same message as before.
