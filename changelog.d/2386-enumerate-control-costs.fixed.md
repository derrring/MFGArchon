- **The evenness that #2375 ruling 5's "nothing in this library changes value" rests on is now checked for every control-cost class in the package** (#2386).
  - `test_conjugate_is_alpha_sign_blind` promised in its docstring that *"if a non-even control cost is ever added, this test fails"*. It could not: it iterated a three-entry hand-written dict, which was already missing `_MoreauYosidaControlCost`. Measured with a deliberately non-even cost added to the library: 38 passed, 0 failed.
  - It is now parametrised over the imported `ControlCostBase` subclasses, and asserts `L(a) == L(-a)` bit-exactly before comparing the two conjugates. The conjugate comparison alone runs through `minimize_scalar` at `abs=1e-6`, and an odd term `eps·a` on a quadratic cost moves it by `2·p·eps/λ`, so it cannot see `eps` below about `λ·6e-8`.
  - `__subclasses__()` only sees classes some module has already imported, and a parametrisation is fixed at collection. So a new test, `test_every_library_control_cost_is_even`, imports every module in a **child interpreter**, enumerates the concrete library costs there, builds each with its default arguments, and asserts `L(a) == L(-a)` bit-exactly at ten values of α and that `effective_domain()` is symmetric — the two conjugates agree only when both hold. An instance built with non-default arguments is not checked, so a class that is even only at its defaults passes. It fails if the child read a different tree, if any module fails to import, or if a class it enumerated was not probed. The sweep runs out of process on purpose: run in the test process, it pre-imported the renamed shim modules for later tests on the same worker, and the warning census could then lose their `DeprecationWarning`s, depending on xdist scheduling.
  - Measured both ways: a non-even cost, or an even one with a one-sided admissible set, in a module collection never imports fails the new test and names the class; an even symmetric one passes; removing the child's import loop makes the non-even case pass again.
  - A red here does not mean "make the cost even". Ruling 5 exists so the library is correct for a non-even cost; it means that class's hand-written closed forms for `H` (`evaluate`, `optimal_control`, `dp`) must be derived under `H = sup{-p·α - L}`, and the "nothing changes value" paragraph no longer holds.
  - The test docstrings that still stated the pre-#2375 pairing `sup{p·α - L}` now state `sup{-p·α - L}` (#2387).
