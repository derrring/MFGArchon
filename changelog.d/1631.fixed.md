- **nD FDM: a MAXIMIZE/non-quadratic Hamiltonian can now use the velocity and callable-drift channels** (Issue #1631, toward #1528 phase 2). `solve_fp_nd_full_system` resolved the scalar drift coefficient unconditionally at function scope, so `fp_drift_coefficient`'s (correct) fail-loud guard for non-quadratic-MINIMIZE Hamiltonians fired *before* the drift channel was consulted -- such a problem could not run even when supplying `velocity_field` or a callable `drift_field`, the channels that exist to carry its precomputed alpha*. The coefficient is now resolved lazily, at the point of use, and skipped entirely where no handler reads it (`divergence_upwind` with a velocity, and the callable-drift path). Byte-identical everywhere it was actually read -- verified across 95 configurations spanning 1D/2D/3D, no-flux/periodic/Dirichlet/mixed BCs, all four advection schemes and legacy aliases, tensor diffusion and source terms. The U-derived channel still fails loud for those Hamiltonians, as it must.
