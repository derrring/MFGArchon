- **nD FDM: a MAXIMIZE/non-quadratic Hamiltonian can now use the velocity channel** (Issue #1631, toward #1528 phase 2). `solve_fp_nd_full_system` resolved the scalar drift coefficient unconditionally at function scope, so `fp_drift_coefficient`'s (correct) fail-loud guard for non-quadratic-MINIMIZE Hamiltonians fired *before* the drift channel was consulted -- such a problem could not run even when supplying `velocity_field`, the channel that exists to carry its precomputed alpha*. The coefficient is now resolved lazily, at the point of use, and is skipped entirely for the scheme that consumes `interface_velocity`. Byte-identical everywhere the coefficient was actually read; the U-derived channel still fails loud for those Hamiltonians, as it must.
