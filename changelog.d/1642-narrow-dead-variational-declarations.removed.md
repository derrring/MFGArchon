- **Dead variational and autodiff declarations narrowed away** (Issue #1642, capabilities A5 + D1; refs #1342). `create_mfg_problem("variational", ..., use_unified=False)` imported `mfgarchon.solvers.variational` -- a module that has never existed -- so a documented public call raised `ModuleNotFoundError` with no guidance. The branch now raises `NotImplementedError` naming the real class (`mfgarchon.alg.optimization.variational_problem.VariationalMFGProblem`), the contract mismatch that prevents forwarding (it takes `xmin`/`xmax`/`Nx` plus `VariationalMFGComponents`, so a `Domain` geometry would fall into `**kwargs` and the domain would silently default to `[0, 1]` with `Nx=51`), the `use_unified=True` alternative, and the blocking issue #1342; the corresponding `@overload` is gone. The same ghost path is removed from `extensions/__init__.py`'s module docstring and from `base_variational.py`'s `TYPE_CHECKING` import, which now points at the real module. Three zero-byte solver packages -- `alg/optimization/augmented_lagrangian`, `primal_dual`, `variational_methods` -- advertised solver families that do not exist and are deleted. `FunctionalDerivativeConfig.backend` (plus the `use_jax` / `use_pytorch` deprecated aliases and the `__post_init__` that only fed them) selected a JAX/PyTorch autodiff backend that `utils/functional_calculus.py` never implements and no caller ever read -- the dataclass is not instantiated anywhere in the repo. **Breaking (pre-1.0)**: the legacy variational factory branch and those config fields are removed outright rather than shimmed.
