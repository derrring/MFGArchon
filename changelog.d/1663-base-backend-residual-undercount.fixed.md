- **A second undercount survived in the same docstring as the one that was fixed** (Issue #1663). `BaseBackend.std` was corrected in #1670 to say "four implementers"; five lines below it, `base_backend.py:113` still read "Population is chosen because **two of the three** already did it". Both numbers are wrong: three of the four (`numpy`, `jax`, `numba`, all `ddof=0`) already used the population convention, and only `TorchBackend` did not. The earlier sweep grepped for the two literal strings already known to be wrong rather than for the claim, so a paraphrase of the same undercount five lines away was missed.
