- **Three stale counts, a dangling commit, and the kill matrix's unchecked prose** (Issue #2349).
  - **The count is no longer restated in prose that nothing reads.** `AGENTS.md`, `scripts/local_ci.sh` and `test_applicator_bc_type_contract_1948.py` each said the matrix has **24** mutations; it has had 26 since #2313 and 27 since #2342. The worst was `local_ci.sh`, where the number was the *evidence* that a grep-based completeness guard cannot miss a mutation — so a stale number was propping up a soundness claim. All three now name the property (`MUTATIONS` declares them, `test_the_mutation_list_matches_the_parametrisation` owns the count) instead of the number.
  - **Left alone deliberately:** `AGENTS.md`'s cost derivation (`baseline_seconds` 198.2 plus 3814.6s over 24 mutations, measured at `2c923694`) and three changelog fragments. Those are quantified over a specific past record, say so, and are re-derivable from the file beside them — the AGENTS.md line even states that property. A figure anchored to immutable history does not rot; one quantified over the current tree does.
  - **The baseline's `_measured_at.commit` is a pre-merge branch sha, and this repo squash-merges**, so it is unreachable the moment the PR lands — main's baseline has cited a dangling `a70d9a06` for weeks (`git branch -a --contains a70d9a06` is empty). The sha stays for provenance, now beside a `reproduce` field giving the procedure that works: `git log --oneline --follow scripts/discrimination_baseline.json`, then the merge commit that last touched it. A broken anchor is worse than none, because a reader tries it, fails, and has nothing else.
  - **`owner` is now checked against the code.** It is recorded in both JSONs and was compared by nothing, while being the only place a reader learns what convention a mutation tests — and every `owner` fixture in the ratchet's own tests is a bare `"x"` placeholder, which is where the check was missing. The review of #2347 corrected two substantive errors in one such string, by hand-editing the artifacts rather than paying a 90-minute sweep for a documentation fix; that edit was correct and unverified until now. The new test fires both ways: an artifact hand-edited into disagreement, and a script reworded without a re-record.
