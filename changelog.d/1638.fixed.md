- **Geometry and shim hygiene** (Issue #1638). `TensorProductGrid`'s docstrings carried 36 dead constructor examples (7 using the removed positional signature, 29 omitting the `boundary_conditions` that #674 made mandatory), including all four in the class-level docstring; executing them also exposed an undefined placeholder, a `BoundaryConditions(bc_type=...)` kwarg that no longer exists, a `mark_region` predicate that collapsed its per-point mask to a scalar, and a `...` stand-in mask. All now run, pinned by a test that executes every example so the rot cannot return. The module's `__main__` smoke block, dead since #674, runs again. `alg/iterative/schedules.py` pointed migrators at `mfgarchon.utils.convergence.schedules`, which does not exist -- retargeted to the real owner `mfgarchon.utils.iteration.schedules` and its stale v0.20.0 deadline moved. The meshless-Galerkin FP docstring still described the pre-#1528 `drift = U` convention and referenced closed issue #1043, though `_build_advection` routes through `H.optimal_control`. `Hyperrectangle`'s SDF no longer claims exact Euclidean distance in the exterior, where it returns the Chebyshev form (1.0 vs the true √2 at (2,2)). An unreachable numba fallback that would have dropped advection entirely is deleted.
