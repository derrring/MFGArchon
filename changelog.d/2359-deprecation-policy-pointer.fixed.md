- **`AGENTS.md`'s deprecation policy pointed at a filename that had named nothing for months** (Issue #2359).
  - The section ended `Ref: DEPRECATION_LIFECYCLE_POLICY.md` — a bare basename, in the file every contributor reads first, for the one policy a removal batch is measured against. #2355 had to state a maintainer's waiver against exactly this policy.
  - **The pointer was broken by a pruning commit, not by the move.** Traced across revisions: `a6569f57` added it as `docs/development/DEPRECATION_LIFECYCLE_POLICY.md`; **`cc21745c` (#852) updated it in the same commit that moved the file**, to `mfg-research/docs/archon-notes/development/…`, so it kept resolving; **`68691252` (#1520, "prune duplication (924→236)") truncated it to the bare filename**. A first draft of this fragment blamed `cc21745c` — the one commit that did the right thing — and was corrected in review by a five-revision trace.
  - **It now cites a git ref, not a path**: `cc21745c^:docs/development/DEPRECATION_LIFECYCLE_POLICY.md`, the last in-repo version, marked historical. A path can rot; a ref cannot. It is *labelled* historical because `git show` on it resolves to something plausible and stale — a decorator example using a `remove_by=` parameter since removed from the system, and `mfg_pde` imports that no longer exist — which is a quieter trap than the dead filename it replaces.
  - **The "four clauses are the whole of the policy" claim is gone, because it was false.** `mfgarchon/utils/deprecation.py` is the *executable* statement: `_removable_by_policy` implements the timeline (and reads "either suffices" where its own docstring says "whichever is longer"), while `check_removal_readiness` gates on removal blockers and equivalence — conditions clause (4) does not name. `docs/user/LEGACY_PARAMETERS.md` publishes a different window again. A draft of this named `_removable_by_policy` as carrying all three; it is the age test only, and citing a function for something it does not contain is the same defect as the misquotation above, two paragraphs apart. #2366 owns reconciling them; `AGENTS.md` now says it is not the only statement and names the others, which is a signpost rather than a fix.
  - **A quotation was attributed to the wrong document, in a PR about citations that cannot be followed.** A draft quoted *"Deprecated with warning but wrong default → 1 month of catastrophic bugs"* as the policy document's verdict. It is not there — **0 hits against an 86-hit control for "deprecat"** in both revisions of that file. It is in `a6569f57:CLAUDE.md:549`. The incident and its figures are real; the attribution was not.
  - Three search failures are recorded on this issue rather than in the governing file, per `AGENTS.md`'s own rule that incident evidence lives in issues and git history: a negative existence claim drawn from one artifact, a quotation defeated by markdown emphasis, and a false negative I diagnosed as a line-wrap when the defeater was query **scope** — the same phrase sits unwrapped in `tests/unit/test_gate_hook.py`.
