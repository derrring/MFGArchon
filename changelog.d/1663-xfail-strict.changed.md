- **`xfail_strict = true`** (Issue #1663). Without it an `xfail` that starts passing is reported as a pass, so a fixed defect never announces itself. Two XPASSes had accumulated, both citing Issue #583, closed 2026-02-05, and removing their markers showed neither was exercising the path its reason named. The RBF one never enters RBF at all: counter instrumentation over its exact configuration records `interpolate_value_rbf_fallback` invoked **0** times against `interpolate_value_1d` **5177** times, because the 1D branch returns `interpolate_value_1d` unconditionally and the only RBF call site is in the nD `else` -- so in 1D the flag is structurally inert (Issue #1664). For the cubic one, **why it now passes is not established**: an earlier draft of this change attributed it to #583's PCHIP dispatch, and that is refuted by counterfactual -- aliasing `PchipInterpolator` to `CubicSpline` leaves the test passing. Both docstrings now record what is measured and stop short of a mechanism nobody has tested.
