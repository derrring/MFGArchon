- **`xfail_strict = true`** (Issue #1663). Without it an `xfail` that starts passing is reported as a pass, so a fixed defect never announces itself. Two XPASSes had accumulated silently, both citing Issue #583, closed 2026-02-05. Removing their markers exposed that neither was exercising the path its reason named: `test_cubic_improves_smoothness` was stale for the ordinary reason -- #583 was fixed by routing `interpolation_method='cubic'` through PCHIP (`hjb_sl_interpolation.py:79-85`), measured at 2976 `PchipInterpolator` constructions against zero `CubicSpline` -- while the RBF test never enters the RBF path at all: counter instrumentation over its exact configuration records `interpolate_value_rbf_fallback` invoked **0** times against `interpolate_value_1d` **5177** times. Both tests are renamed to state what they actually verify.
